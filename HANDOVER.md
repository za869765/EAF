# 專案接力記錄 — 2026/07/07（ACC v4.8.0）

> 換台電腦繼續：先 `git -C .../EAF pull`。暗號「**佳里ACC，接力**」。

## 本次完成（v4.8.0）— 用途說明重複把關（非阻擋動畫提醒）
- **需求**：動支/請購單填「用途說明」時，若與歷史上**其他編號**的單子一模一樣，跳 CSS 動畫、明顯顏色文字彈窗提醒，但**不強制阻擋**存檔。
- **做法（index.html）**：用途說明欄 `#purpose-desc` 加 `onblur="checkDupPurposeDesc()"`。`findDupPurposeDesc()` 掃 `cachedRecords`，比對 `purposeDesc`（trim 後完全相同）且屬其他編號（排除自己 id/voucherNo、排除作廢、排除空說明）；有中則 `showDupWarn()` 顯示橘色警示彈窗 `#dup-warn-popup`（`dupWarnIn` 彈入 + `dupWarnPulse` 脈動 CSS 動畫），列出重複憑證編號/類型/日期，點一下或 6.5 秒自動關。純提醒。
- **子版號進位**：v4.7.9→v4.8.0（規則：子版到9進位）。
- **驗證**：jsdom 抽真函式 10/10（多筆重複、排除作廢、編輯自己不誤報、獨特說明、空白、前後空白 trim）。

---

# 專案接力記錄 — 2026/07/07（ACC v4.7.9）

> 換台電腦繼續：先 `git -C .../EAF pull`。暗號「**佳里ACC，接力**」。

## 本次完成（v4.7.9）— 公務預算(21020301)用途別＝業務費 + 動支單多用途拆分
- **需求**：使用者提供 115/7 支出憑證報核單（業務費）。要把公務預算 21020301 的用途別由單一「99 其他」改為業務費各項，且**一張動支單可拆多個用途、各填金額**，之後重新拆分舊資料讓 ACC 分得清。**限 21020301，其他科目一律不動**。
- **用途別**：subjects.json ＋ **D1 settings.subjects**（前端先讀 /api/subjects 即 D1，靜態檔只是 fallback，故兩邊都改；D1 已用 wrangler `--file` UPDATE 外科式只換 21020301 purposes、其餘 14 個 businessPlan 不動）。20 項業務費（電費/水費/通訊費-數據/通訊費-一般/房屋建築修繕/兵勤車租金/辦公器具養護費/一般事務費/其他業務租金/資訊操作維護費/設施及機械設備養護費/辦公廳舍保險/土地租金/稅捐及規費/保險費/油料/車輛養護費/國內差旅費/教育訓練費/國內組織會費）＋99，**code＝名稱**（使用者選）。
- **動支單拆分（index.html）**：僅 `isPubBudgetExpense()`（formType=expense 且 sel-bp=21020301）時，每動支明細列多一個「用途別」下拉——`.pub-cell` 平時 `display:none`、body 加 `pub-mode` 時顯示（**免重建列、其他單據零影響**，隱藏空 cell 不列印）。`getFormData` 由各列 purpose 彙總 `purposeSplits:[{name,amt}]`，多用途→purposeName「多用途」、purposeDesc 自動彙總；`fillForm` 還原每列用途別。後端 records.js 無 schema（整筆 JSON 存 data 欄），故 purposeSplits 自動 persist、免改後端。
- **acc.html 公務預算分頁**：loadPubRecords 篩選改**僅 bpCode==21020301**（拿掉 purposeName 開頭 99 限制，舊 99 記錄照顯示待重拆）；逐筆顯示用途別拆分明細＋新增「🧮 依用途別彙總（年度）」chips。
- **admin.html**：記錄詳情加「用途別拆分」列。**傳票免改**（21020301 不進 510601/110404 傳票產製流程，已確認）。
- **驗證**：vm 抽 index.html 真 `getFormData` 灌 3 列（電費9501/水費8796/電費追加500）→ 電費彙總 10,001、多用途判定、單一用途 fallback 全對，10/10。⚠️ **UI/列印外觀尚未於瀏覽器實測**（下次可在線上站台選 21020301 動支單確認用途別欄顯示、拆分、列印）。

## 待辦
- v4.7.9 UI 線上目視確認（21020301 動支單用途別欄/拆分/報核單列印）
- 114 銀行專戶期初 initVal 單一 localStorage（已知小限制）

---

# 專案接力記錄 — 2026/07/07（ACC v4.7.8）

> 換台電腦繼續：先 `git -C .../EAF pull`。暗號仍為「**佳里ACC，接力**」，暗數：庫存6月 108,809、應付費用 814,221、財產4月淨值 78,433、醫療基金專戶115期初 4,897,987。

## 本次完成（v4.7.8）— 銀行專戶期初改分年度
- **問題**：`renderBankDeposit` 的 `initVal` 讀單一 localStorage（`acc_bank_medical/custody/health_init`，非分年度），切到 114 年度時期初仍套 115 值→114 各月餘額全錯（HANDOVER 舊列的「已知小限制」）。編輯 UI（initbal-* 卡）早已從 HTML 移除、只剩被 guard 的 dead JS，故 initVal 的唯一活躍消費者就是 renderBankDeposit。
- **做法**：新增 `getBankOpeningBalance(y,acct)` — 取該年度**年初結轉傳票 `{y}300001`** 本專戶淨額（借−貸）為期初（自然分年、免手動輸入；結轉傳票本就被 `isYearOpeningVoucher` 排除在異動外，不雙計）；無結轉傳票時後備讀舊 localStorage。月別彙總「期初」列標籤改動態 `${y-1}年度結轉`（原寫死 114）。
- **D1 實測**：115 期初＝4,897,987（＝原 localStorage，**115 零回歸**）；114 期初＝5,388,674、保管款 415,593（**修正**）；健保專戶兩年皆 0（後備）。114 含年終結轉故 11412 期末=0（完整封閉）、中間月看累積＝正確會計。
- **驗證**：vm 抽真原始碼＋真 D1 資料 9 項全過（分年期初、後備、只認結轉傳票、端到端期末=期初+異動）。見 scratchpad/smoke-bank.js。

## 待辦
- （目前無明確待辦）先前三項（公務預算40,000年結轉、立沖負值、財產匯入）皆已處理；210202 應付帳款 v4.7.6 獨立分頁、財產匯入 v4.7.7、銀行分年期初 v4.7.8。若要再深入：財產若某月有本期增加(新購)導致原始成本變動，存摺「原始成本」欄仍寫死 320,557（目前各月增加皆0、無影響）。

---

# 專案接力記錄 — 2026/07/07（ACC v4.7.7）

> 換台電腦繼續：先 `git -C .../EAF pull`。暗號仍為「**佳里ACC，接力**」，暗數：庫存6月 108,809、應付費用 814,221、財產4月淨值 78,433。

## 本次完成（v4.7.7）— 財產分類量值統計表匯入（官方結存成為權威）
- **檔案格式（實測）**：財產分類量值統計表 .xls（BIFF/OLE2）SheetJS 直接讀出乾淨數字，「逐位數欄（兆千百…元）」只是**畫面表頭**、底層是單一數字，解析比 HANDOVER 舊記的「需另寫解析」簡單很多。每檔 4 sheet 只有 Table0 有料。使用者提供 115/01-04 四檔，只有**什項設備**有金額（帳面淨值逐月折 2,193：85,012→82,819→80,626→**78,433**）；房屋7/機械1/交通12 只有數量無值。
- **三方對帳已 tie**（D1 核）：官方報表結存 ＝ 折舊存摺(計算) ＝ 傳票側(130601成本−130603累折)，逐月吻合（6月都是 74,047＝首頁值）。機械130401/403、交通130501/503 成本＝累折、淨值0。
- **做法（使用者選「官方值成為權威」）**：財產分頁加「📊 匯入財產分類量值統計表」→ `onPropReportUpload` 解析什項設備逐月本期結存→存 D1 `acc_prop_official`（`{ym:{carry,add,sub,depr,bal}}`，多選/跨月/重匯覆蓋）＋「🗑️ 清除官方資料」。`getPropNetThroughMonth` 帳面淨值優先讀官方、缺月以每月折舊計算延伸（**官方月把計算漂移拉回基準**）；`getPropDeprForMonth` 官方折舊優先。折舊存摺逐月標「官方/計算」、`updateDeprBalance` 期末淨值走官方。**未匯入前行為＝舊版純計算**（回退安全）。
- **驗證**：vm 抽 acc.html 真原始碼＋真 4 檔實跑 28 項全過（解析、官方權威、缺月計算 05=76,240/06=74,047、亂折舊時官方拉回、render 標籤、清除還原）。見 scratchpad/smoke-prop.js。
- ⚠️ 已知小限制：`PROP_INIT_BAL_YEAR_START=87205`、原始成本 320,557 仍寫死（115 年適用）；若某月有本期增加(新購)導致成本變動，「原始成本」欄不會變（目前各月增加皆0，無影響）。跨年度年初值非分年，同折舊存摺既有限制。

## 待辦（未動）
- 114 年度視圖：銀行專戶期初 initVal 為單一 localStorage（非分年度）— 已知小限制
- 應付帳款 210202 已於 v4.7.6 做獨立分頁（見下）；財產匯入已於本版完成

---

# 專案接力記錄 — 2026/07/07（ACC v4.7.6）

> 換台電腦繼續：先 `git -C .../EAF pull`。暗號仍為「**佳里ACC，接力**」，暗數：庫存6月 108,809、應付費用 814,221。

## 本次完成（v4.7.6）— 新增「應付帳款 210202」獨立分頁
- **背景**：210202 應付帳款（進貨／雜支賒購掛帳，借 110404 醫療用品 或 510601 成本／貸 210202）先前在 App 完全沒露出——`loadPayable` 硬篩 `210205`、`loadCustodial` 用 8 碼 code 開頭比對，6 碼的 210202 兩邊都抓不到；首頁負債卡也只有應付費用/代收款/公務預算。淨額懸空。使用者選「獨立新分頁」呈現。
- **做法**：新增分頁「📑 應付帳款」（按鈕在代收款後、tab-payacct pane）。`loadPayAcct`/`renderPayAcct` 比照 v4.7.5：**立帳（累計應付，未沖）vs 已沖抵（已支付）兩區**、卡頭大字「剩餘＝立−沖」、**年度隔離**（僅本年度 ym 前3碼＝sel-year、截至選定月）、負剩餘警告、**孤兒沖帳略過**（本年無立帳的支出沖銷，多為去年立帳今年沖）、空 ym 排除、CSV 匯出（`exportPayAcctCsv`）。純呈現不寫資料。
- **接線**：`TAB_RENDERS.payacct`、`onYmChange`、傳票產製/匯入/刪除/init 各刷新點都加 `renderPayAcct()`。三檔版次 v4.7.5→v4.7.6。
- **驗證**：⭐vm 抽 acc.html 內**實際原始碼**灌 mock DOM+真資料實跑，28 項全過（立沖計算、114/空ym/未來月/他科目排除、孤兒略過、負剩餘警告、空狀態、截止日）。見 scratchpad/smoke-payacct.js。
- **✅ 已用 D1 正式資料核對（wrangler d1 execute eaf-records --remote）**：210202 共 44 筆（114 淨0、115 淨 194,006＝立 257,325−沖 63,319）。用 acc.html 實際 loadPayAcct 餵真資料逐月跑，剩餘精準 tie：3月0／4月34,904／5月150,690／**6月起 194,006**（＝暗數）。跨年期初無虞——`11501 115300001#12 貸14,135「上年度結轉本年度」`確實在 D1，年度隔離下已含此結轉立帳、不低估。115 有立帳故無孤兒沖帳略過。（本機 wrangler 已 OAuth 認證 za869765，含 d1 write，可直接查 D1）。

## 待辦（未動）
- **財產管理匯入**（財產分類量值統計表，逐位數欄格式需另寫解析；只有 11504/11505 兩月）
- 114 年度視圖：銀行專戶期初 initVal 為單一 localStorage（非分年度）— 已知小限制

---

# 專案接力記錄 — 2026/07/04（ACC v4.7.2）

> 換台電腦繼續：先 `git -C .../EAF pull`，讀完最上面再繼續。暗號仍為「**佳里ACC，接力**」，暗數：庫存6月 108,809、應付費用 814,221。

## 本次已完成並上線（v4.7.2–v4.7.5）
- **跨年度隔離（重要）**：使用者曾匯入 114年整年傳票（464張/2292筆，D1 現有 3172 筆），導致 115 對帳毛額爆增（應付費用立帳 173萬→716萬約4倍）。根因＝`loadPayable`/`loadCustodial`/`renderBankDeposit`/`renderInvVoucherRecon` 只用「ym≤截止月」未限年度，字串比較下 `11401≤11506` 成立→114 全年漏進 115。修法：四處加「ym 前3碼＝本年度」過濾（沿用頂部年度選單 sel-year）。**114 資料保留**，可切年度單獨看；115 已回驗證值、零洩漏（jsdom 語法+真資料雙驗證通過）。有用 yearPrefix 的視圖（累積賸餘/應收/收入掛帳/備抵折讓）本就安全。
- **代收款薪資分卡改用 sub_account（issue 2）**：21020321 原依 description 分卡，年結轉描述皆「上年度結轉本年度」→ 6 筆保險別結轉(184,053)全落「其他」，公健/勞健淨餘為負(−30,717/−21,240)。改用 sub_account（01公保費…06勞工退休準備金）主判、description fallback → 全轉正(公健61,001/勞健8,420…)、群組淨額不變(249,786)。應付費用210205 本就用 sub_account 正常，未動。
- **補漏抓代收款科目**：年結轉裡 21020313 心健科(淨+18,000)、21020303 加班費(淨0) 因未定義群組被整筆漏抓，補進 CUST_GROUPS_DEF。代收款總額 533,840→**551,840**。
- **issue 1（公務預算40,000）已釐清不動**：官方立沖帳明細確認該 40,000＝傳票 114100146「114年12月門診醫生應診費」，已由 115200005(10,000)+115200009(30,000)沖平、未沖餘額0；我方 D1 記在 115300001 年結轉、兩筆沖帳也都在，立40,000抵沖40,000=0，21020301 淨69,642＝官方三筆未沖(0+27,169+42,473)。維持現狀，勿排除（排除反破帳）。
- **v4.7.3 庫存對帳改以傳票/官方為準**：使用者匯入官方醫療用品明細表 FNWFRB0150（115/5底=100,682、6底=108,809）確認傳票側 110404＝108,809 正確。手動明細帳（102,745，衛材幾乎空＋藥品缺6月＋此機無 v4.7.1 DPReport3 校正）改標「僅供參考」、不符不再跳橘色告警（renderInvVoucherRecon 邊框固定綠、傳票數字放大為權威）。⚠️手動帳存 localStorage（loadInvLedger 只讀 localStorage，D1 settings 只是備份），如要一致需 DPReport3 藥品/衛材月報表逐月校正，此機沒有那些檔。FNWFRB0150 只有 110404 合計無法逐項校正。
- **v4.7.4 首頁各分頁總覽資產卡全改傳票推導（年度隔離）**：集中在 `updateHomeDashboard`。醫療用品 110404(借−貸)=108,809(=官方)取代手動102,745；財產 130x01−130x03=74,047 取代寫死排程(真實傳票累折記 130603)；備抵 110306(貸−借)=57,152。rc-inv-total/rc-property 不再由 updateInvTotal/updateDeprBalance 覆寫；TAB_RENDERS 加 `home`。財產「頁面」內部(getMonthlyDepr 也餵月底結算)未動、115本就對。
- **v4.7.5 應付費用/代收款 立沖改兩區呈現**：`renderPayable`/`renderCustodial` 每科目(代收款含各保險別)拆「①立帳明細(累計應付/未沖) ②已沖抵明細(已支付)」，卡頭大字「剩餘(未沖)＝立−沖」。純呈現、資料不變。⭐無 jsdom 驗證法：vm mock DOM 灌真資料實跑 render(見 scratchpad/smoke.js)。

## 待辦
- **財產管理匯入**（逐位數欄格式，使用者說改天做）
- **應付帳款 210202**（淨194,006，應付費用/代收款兩分頁都沒顯示，屬另一科目，另議）
- 114 年度視圖：114 含年終結轉，故 11412 截止時淨額為0（完整封閉），中間月份可看累積；如需 114 各月銀行專戶期初，initVal 目前是單一 localStorage（非分年度），屬已知小限制

---

# 專案接力記錄 — 2026/07/03（ACC 會計帳務）

> 換台電腦繼續時：先 `git -C .../EAF pull`，讀完最上面這段再繼續。

## 🔑 接力暗號：「**佳里ACC，接力**」
> 使用者一說出這句 ＝ 立刻：① `git pull` ② 讀本檔最上面 ③ 口頭報告下方「待議三項」④ 問要從哪一項開始。
> 對得上脈絡的暗數：**庫存6月合計 108,809**、**應付費用 814,221**。（若新視窗沒認出暗號，就叫它「git pull EAF、讀 HANDOVER.md」）

## 本次已完成並上線（Cloudflare Pages 自動部署）
- **v4.7.0**：acc.html 新增「📐 收支對帳」分頁 — 整合單檔工具「基金收支對帳試算」，核對「收支餘絀表↔醫療獎勵金計算表」恆等式（業務賸餘＝5%提撥＋加班費抵繳±差異R−CD）。命名空間全隔離（`window.Recon` / `#tab-recon` / `rcn-` id / CSS scoped）。
- **v4.7.1**：acc.html 醫療用品庫存分頁新增「📊 匯入庫存月報表（校正結存）」— 上傳 DPReport3（藥品/衛材庫存月報表 .xls），自動把明細帳「每月結存」以「盤盈/盤損」校正到官方盤點數。分類：藥品代號含英字母→藥品、純數字→衛材；校正列 desc 標記「［庫存月報表校正］」，冪等可重匯、可作廢。
  - 已驗證 115/01–06 藥品+衛材每月結存精準落點；6月合計 **108,809**（藥品106,051＋衛材2,758）＝傳票110404帳面，分毫不差。

## 對帳結論（已驗證，供接手參考）
- 傳票已匯入（FNWFRX0090，880筆，全略過＝早已在庫）。**應付費用 814,221 / 應付代收款 533,840 / 累積賸餘 3,244,043 全部與官方明細帳相符**。
- 醫療用品原本手動帳 102,745 是錯的（漏登），正解 108,809（v4.7.1 匯入即校正）。

## 資料檔（都在 `D:\Backup\Downloads\`，換機需重抓）
- `FNWFRX0090_*.xlsx` = 傳票明細表（全年傳票；ACC 傳票匯入來源）
- `FNWFBB0070*.xlsx` ×11 = 明細分類帳（代收款/應付費用/國健/疾管/心健/秘書室 官方餘額）
- `DPReport3_1_*.xls` ×14 = 藥品衛材庫存月報表 11501–11506（藥品、衛材各一檔）
- `20260703T*.xls` ×2 = 財產分類量值統計表 11504、11505（其餘月份待補）

## ⏳ 待議／待辦（假日繼續）
1. **公務預算(21020301) 上年結轉 40,000**：年度結轉傳票 115300001 有「上年度結轉本年度 貸40,000」。使用者認為公務預算(周轉金)不應有年結轉 → 需決定是否從應付代收款排除此結轉，或屬來源傳票歸類問題。（既有 renderCustodial／來源資料，非本次改動）
2. **立沖帳淨餘為負**（例：勞健 立18,008−沖39,248＝−21,240）：使用者「有多少才能沖多少」。成因＝今年沖去年立帳(年結轉)，但群組只加本年立帳 → renderCustodial/renderPayable 需把 115300001 年結轉立帳併入對應子目，或改對帳邏輯。
3. **財產管理匯入（比照庫存）**：財產分類量值統計表為「逐位數欄」格式（兆千百…元每位一欄），需另寫解析；目前僅 11504、11505，其餘月份待補下載。

## 版次規則（沿用）
- acc.html / admin.html / index.html 三檔版次同步；每次改動 commit＋push（自動部署）。
- index.html 內 `/* v4.6.x … */` 是歷史變更註解，非版本徽章，勿一起 bump。

---

# 專案接力記錄 — 2026/04/05

> 明天說「繼續」時：先 `git pull` 拉取此檔，讀完後主動說明各項進度，再問從哪個開始。

---

## 📌 各專案目前狀態

### 1. EAF 動支管理系統 + ACC 會計帳務
**路徑:** `C:/Users/USER/Desktop/CODE/project/EAF`
**GitHub:** `za869765/EAF`
**版本:** `v4.0.1`（acc.html / admin.html / index.html 三檔同步）
**部署:** Cloudflare Pages 自動部署

#### ✅ 04/05 完成（v3.9.4 → v4.0.1）
| 版本 | 內容 |
|------|------|
| v3.9.5 | 首頁移除年度收支累積區塊 |
| v3.9.6 | 產製傳票同步更新 fund5 至 acc_yeardata |
| v3.9.7 | 修正1月fund5 277＋正名醫療作業基金提撥 |
| v3.9.8 | 修正3月fund5 3740 |
| v3.9.9 | 強制覆蓋Q1 fund5種子值＋flag 302 |
| v4.0.0 | 支出傳票上傳沖銷（應付費用/代收款/累積賸餘） |
| v4.0.1 | 互動式預覽＋99其他獨立分類＋02a/02b智慧拆分 |

#### ⏳ 待辦
- [ ] 支出傳票沖銷實際測試（4月底5月初）
- [ ] 防重複上傳機制
- [ ] 衛生保健拆分完整測試（預計5月有資料）
- [ ] 收入掛帳分類細拆（藥品/門診/預防保健）
- [ ] 4月轉帳傳票產製測試

#### 重要設計規則
- 每次修改三檔版次同步
- 版次進位：子版號到9就進位（v3.9.9→v4.0.0）
- 每次改動必須 commit + push
- 獎勵金計算表公式文件：`docs/115獎勵金計算表_公式說明.md`

---

### 2. GIS 佳里區 GIS 查詢系統
**版本:** v6.436 | **狀態:** 穩定運作中

### 3. CLASS 系統
**狀態:** 🆕 尚未開始

---

## 🚀 接力步驟

1. 讀取本檔 `C:/Users/USER/Desktop/CODE/project/EAF/HANDOVER.md`
2. `git -C C:/Users/USER/Desktop/CODE/project/EAF pull`
3. `git -C C:/Users/USER/Desktop/CODE/project/EAF log --oneline -10`
4. 向使用者報告進度，問從哪個開始
